>Install requirements listed in requirements.txt

>create "testdb" (command: mysql > CREATE DATABASE testdb)

(May need to run command: sudo chown -R _mysql:mysql /usr/local/var/mysql)

sudo mysql.server start
